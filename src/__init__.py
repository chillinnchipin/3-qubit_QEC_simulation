from .utils import set_debug_mode, set_verbose_outputs

# initializes the module with debug mode and verbose set to false, will change later if arguments specify
set_debug_mode(to_set=False)
set_verbose_outputs(to_set=False)
