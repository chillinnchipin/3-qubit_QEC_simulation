from .utils import set_debug_mode, set_verbose_output

# initializes the module with debug mode and verbose set to false, will change later if arguments specify
set_debug_mode(debug=False)
set_verbose_output(verbose=False)